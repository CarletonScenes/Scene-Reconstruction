cmake_minimum_required(VERSION 2.8)
project( FMTest )
find_package( OpenCV 3 REQUIRED )
include_directories( ${OpenCV_INCLUDE_DIRS} )
file(GLOB SRCFILES *.cpp)
add_executable( FMTest ${SRCFILES})
target_link_libraries( FMTest ${OpenCV_LIBS} )