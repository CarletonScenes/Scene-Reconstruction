cmake_minimum_required(VERSION 2.8)
project( FDTest )
find_package( OpenCV 3 REQUIRED )
include_directories( ${OpenCV_INCLUDE_DIRS} )
file(GLOB SRCFILES *.cpp)
add_executable( FDTest ${SRCFILES})
target_link_libraries( FDTest ${OpenCV_LIBS} )